include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Generated gRPC stubs (tool/gen_proto.sh) are not linted.
    - lib/src/generated/**

linter:
  rules:
    prefer_const_constructors: true
    prefer_final_locals: true
    avoid_print: true
